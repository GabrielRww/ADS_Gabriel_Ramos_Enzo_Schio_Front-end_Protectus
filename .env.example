# Backend base URL
VITE_API_URL=https://protectus-back-end-production.up.railway.app

# When false or unset, the app will NOT call /policies endpoints.
# This prevents 404 noise and preflight/cache buildup on servers without those routes.
VITE_POLICIES_API=false
