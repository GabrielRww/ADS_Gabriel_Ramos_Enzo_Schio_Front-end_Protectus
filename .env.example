VITE_API_URL=https://protectus-back-end-production.up.railway.app
VITE_POLICIES_API=false

# Optional profile API (disable by default to avoid calling missing routes)
VITE_PROFILE_API=false
# Customize these if your backend uses different paths
# e.g., /users/cliente/{id} for PATCH and /users/cliente/{id}/avatar for POST
VITE_PROFILE_UPDATE_PATH=/users/cliente/{id}

# Google Maps
# Cole aqui a sua API Key (recomenda-se restringir pelo domínio)
VITE_GOOGLE_MAPS_API_KEY="AIzaSyBWKr2GGry7vcMR3FvHtK5X2PsDq7x7v9o"

# Prefixo opcional para as rotas de veículos (marcas/modelos/anos)
# Ex.: "/veiculos" resultará em /veiculos/marcas, /veiculos/modelos, /veiculos/anos
VITE_VEHICLE_API_PREFIX=""

# (Opcional) Se o catálogo de veículos estiver em outro host/base
# Ex.: "https://api.seguradora.com" (sem barra no final)
VITE_VEHICLE_API_URL=""

# (Opcional) Se os caminhos diferirem, sobrescreva-os aqui
# Exemplos: "/veiculos/marcas", "/api/veicular/brands"
VITE_VEHICLE_MARCAS_PATH="/insurances/marcas"
VITE_VEHICLE_MODELOS_PATH="/insurances/modelos"
VITE_VEHICLE_ANOS_PATH="/insurances/anos"

# (Opcional) Se os nomes dos parâmetros diferirem do padrão
# Padrão: marca, modelo
VITE_VEHICLE_PARAM_BRAND=""
VITE_VEHICLE_PARAM_MODEL=""
VITE_AVATAR_UPLOAD_PATH=/users/cliente/{id}/avatar

# (Opcional) Se o serviço de veículos exigir um header diferente (API Key ou Bearer customizado)
# Use {token} para injetar o JWT atual do usuário, ex.: "Bearer {token}"
VITE_VEHICLE_AUTH_HEADER=""
VITE_VEHICLE_AUTH_VALUE=""

# Endpoints de login (caso sejam diferentes do padrão)
VITE_LOGIN_CLIENTE_PATH=/auth/login-cliente
VITE_LOGIN_FUNCIONARIO_PATH=/auth/login-funcionario